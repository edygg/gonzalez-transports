<%= form_for(@schedule, class: "form-horizontal") do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :departure_city_id, class: "control-label" %><br>
    <%= f.select :departure_city_id, options_from_collection_for_select(City.all, :id, :name), class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :arrival_city_id, class: "control-label" %><br>
    <%= f.select :arrival_city_id, options_from_collection_for_select(City.all, :id, :name), class: "form-control" %>
  </div>  

  <div class="form-group">
    <%= f.label :departure_time, class: "control-label" %><br>
    <%= f.time_select :departure_time, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
